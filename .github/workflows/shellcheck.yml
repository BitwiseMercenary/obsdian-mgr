name: shellcheck

on:
  push:
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # shellcheck is pre-installed on GitHub's ubuntu-latest runners.
      - name: Run shellcheck
        run: shellcheck obsidian-mgr
